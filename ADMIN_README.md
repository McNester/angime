# Админ-панель для управления меню Әңgіме

## Описание

Админ-панель позволяет управлять меню сайта без необходимости редактировать код напрямую. Вы можете добавлять, редактировать и удалять позиции меню через удобный веб-интерфейс.

## Доступ к админке

Админка доступна по адресу: **`/adminangime`** или **`/adminangime/`**

### На продакшене (angime.org.kz):
**`https://angime.org.kz/adminangime`**

### Локально:
- Через файловую систему: `file:///путь/к/проекту/adminangime/index.html`
- Через локальный сервер: `http://localhost:8000/adminangime`

### Использование:
1. Откройте админку в браузере по адресу `/admin`
2. Введите пароль (по умолчанию: `angime2024`)
3. Нажмите "Войти"

**⚠️ ВАЖНО:** Измените пароль в файле `admin.js` (строка 3):
```javascript
const ADMIN_PASSWORD = 'ваш_новый_пароль';
```

## Функции админки

### Управление позициями меню

- **Добавить позицию**: Нажмите кнопку "+ Добавить позицию" в выбранной категории
- **Редактировать**: Нажмите "✏️ Редактировать" на карточке позиции
- **Удалить**: Нажмите "🗑️ Удалить" на карточке позиции (с подтверждением)

### Редактирование позиции

При редактировании или добавлении позиции вы можете указать:

- **Название** (RU, ҚЗ, EN) - обязательное поле для RU
- **Цена** - обязательное поле (например: `1700₸`)
- **Описание** (RU, ҚЗ, EN)
- **Характеристики** - через запятую (например: `400мл, Светлое`)
- **Изображения** - пути к файлам, одно на строку или через запятую
- **Дополнительные детали** - JSON формат (например: `{"Объем": "400мл"}`)

### Сохранение изменений

1. Нажмите кнопку "💾 Сохранить изменения"
2. Файл `menu.json` будет скачан на ваш компьютер
3. Загрузите этот файл в корневую папку сайта (рядом с `index.html`)
4. Сайт автоматически будет использовать `menu.json` вместо `config.js`

### Экспорт JSON

Кнопка "📥 Экспорт JSON" позволяет скачать текущее меню в формате JSON для резервного копирования.

## Структура данных

### menu.json

Файл `menu.json` имеет следующую структуру:

```json
{
  "menu": {
    "categories": [
      {
        "id": "beer",
        "titleKey": "menuBeerTab",
        "items": [
          {
            "nameKey": "beerItem1Name",
            "price": "1700₸",
            "descKey": "beerItem1Desc",
            "specs": ["400мл"],
            "images": ["pictures/отработка меню/пиво аниме.JPG"],
            "details": {
              "Объем": "400мл"
            }
          }
        ]
      }
    ]
  },
  "translations": {},
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

## Как это работает

1. **Загрузка данных**: 
   - Сайт сначала пытается загрузить `menu.json`
   - Если файл не найден, используется `CONFIG.menu` из `config.js`

2. **Приоритет данных**:
   - Если `menu.json` существует, он имеет приоритет над `config.js`
   - Это позволяет обновлять меню без изменения исходного кода

3. **Переводы**:
   - Переводы хранятся в `TRANSLATIONS` в `config.js`
   - При добавлении новых позиций через админку, переводы нужно добавлять вручную в `config.js`
   - Или использовать поле "Название" и "Описание" для всех языков в админке

## Безопасность

- ⚠️ Админка защищена простым паролем
- ⚠️ Для продакшена рекомендуется:
  - Изменить пароль на сложный
  - Добавить дополнительную аутентификацию
  - Ограничить доступ к `admin.html` через веб-сервер
  - Использовать HTTPS

## Рекомендации

1. **Резервное копирование**: Регулярно экспортируйте меню через кнопку "Экспорт JSON"
2. **Тестирование**: После изменений проверяйте сайт перед публикацией
3. **Изображения**: Убедитесь, что пути к изображениям корректны
4. **Переводы**: При добавлении новых позиций обновляйте переводы в `config.js`

## Устранение неполадок

### Админка не загружается
- Проверьте, что файлы `admin.html`, `admin.js`, `admin.css` находятся в корне проекта
- Убедитесь, что `config.js` загружается корректно

### Изменения не отображаются на сайте
- Убедитесь, что `menu.json` загружен в корневую папку сайта
- Проверьте консоль браузера на наличие ошибок
- Очистите кеш браузера (Ctrl+F5)

### Ошибки при сохранении
- Проверьте формат JSON в поле "Дополнительные детали"
- Убедитесь, что все обязательные поля заполнены

## Поддержка

При возникновении проблем проверьте:
1. Консоль браузера (F12)
2. Сеть (Network tab) для проверки загрузки файлов
3. Структуру `menu.json` на соответствие формату
